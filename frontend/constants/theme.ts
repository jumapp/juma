/**
 * Legacy theme exports bridged with new design tokens.
 */

import { lightPalette, darkPalette, fontFamilies } from '@/design/tokens';

export const Colors = {
  light: lightPalette,
  dark: darkPalette,
};

export const Fonts = fontFamilies;
